# Copy this file to .env and fill in your values.
# .env is git-ignored, so your secrets stay local.

# BGG account credentials, used to log in for scraping the plays pages
# (which require authentication since fall 2025).
BGG_USERNAME=your_bgg_username
BGG_PASSWORD=your_bgg_password
